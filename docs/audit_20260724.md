# 監査記録: 休眠パス + gemma4 前提残滓（2026-07-24）

LLM re-rank の no-op バグ（LOG.md 2026-07-24）を契機に、同型の「実装済みだが実行されないパス」と
「gemma4（小型・短コンテキスト・弱推論）前提の設計が glm-5.2 移行後の実情に合っていない箇所」を
全 scripts/（85 ファイル、.bak/tests 除く）で監査した結果。呼び出し元は全数 grep で検証済み。

対応パッケージ: **A**=即日低リスク（2026-07-24 実施）／**B**=Patrol 検出器有効化（PM 判断）／
**C**=評価駆動の近代化（2026-07-27 に 6/6 完了）／**D**=小粒清掃（2026-07-27 実施）。
進行状況は PLAN.md、各判断の経緯は LOG.md 参照。D のうち監査の前提が誤っていた 2 件:
「Web の terminology/glossary 削除不可」は save フローの deleted フラグで削除 UI 実装済み
（孤立していたのは別系統の単発 /delete エンドポイント → 削除で決着）、「fish TTS が到達不能」は
別ホスト運用の現役設計（/argus-narrate の英語話者クローンが依存、不通時自動フォールバック）で
残置し default_text_limit() の配線のみ修正。

---

## Part 1: 休眠パス（実装済みだが実行されない）

### 危険（挙動がラベルと逆）
| 項目 | 場所 | 状態 |
|---|---|---|
| `slack_pipeline.py --dry-run` が no-op（付けても DB 書き込み） | data-pipeline/slack_pipeline.py:85 | **A で修正** |
| `call_argus_llm(think=True)` が rivault ルートで無言破棄（call_rivault に think 引数なし。Kimi は thinking 無効化不可のため引数追加ではなく明示化で対応） | utils/llm.py:451,460 / 影響: ingest/slack.py:451,506,526,677・generate_minutes_local.py:1192 | **A で明示化** |

### 機能が眠っている
| 項目 | 場所 | 状態 |
|---|---|---|
| Patrol 検出器 3 種が config 無効（overdue_reminder / deadline_warning / stale_detection、実装完全） | patrol/detect.py:190,224,322 + data/patrol_config.yaml | **B（PM 判断待ち）** |
| `build_risk_prompt()` 58 行が孤立（risk の assignee/topic フォーカス + knowledge_summary 注入が現行経路から脱落。brief/draft 側は生存） | argus/pm_argus.py:918 | D 候補（削除 or 配線） |
| Web UI から terminology/glossary の削除不可（API は完成、フロントにボタン無し） | web/pm_api.py:314,359 vs static/pages/*.js | D 候補 |
| `_LLM_ENV_PREFIXES` に LOCAL_OCR_ 欠落 → web デーモン経由ジョブで図 OCR が黙ってスキップ | utils/llm.py:19 | **A で修正** |
| `retrieve_knowledge_for_extraction` の議事録側呼び出しがコメントアウト（Slack 側のみ生存） | generate_minutes_local.py:1172 | C 候補（復活可否） |
| pm_tts `default_text_limit()` 未配線（fish 400字上限が効かず常に 200字） / fish バックエンド全体がデーモン経路で到達不能（2026-06-11 移行の残骸） | tts/pm_tts.py:445,49 | D 候補 |
| `goals_print.py` due_label/due_label_md 未配線（期限バッジ非表示）。スクリプト自体も孤立 | reporting/goals_print.py:58,80 | D 候補 |
| 飾り引数: pm_users_sync `--from-api`（未参照・結果同一）、generate_minutes_local `--model`（未参照、docstring は必須と誤記） | 各所 | D 候補 |
| Web「screen」ジョブが `--semantic` を渡さない（プレビュー API は既定 True なのに実行ジョブでは意味的重複検出が不発） | web/web_admin.py:239 vs pm_api.py:865 | D 候補 |
| `gather_knowledge()` / `run_explorer_suite()` / `get_aliases()` / `get_slack_client()` / `_parse_bullets()` / pptx_theme の callout/progress_bar/add_paragraph_text — 呼び出し元ゼロ | 各所 | D 候補（配線 or 削除） |
| llm.py `_load_llm_routing_priority` の docstring「後方互換モード」は実装に存在しない（None→即 RuntimeError）。pm-argus-config-schema スキルの route 名 `claude_code` は現コードでは ValueError | utils/llm.py:407,423 | D 候補（doc 修正） |

## Part 2: gemma4 前提の残滓（優先度順）

1. **議事録 Stage 1 `max_tokens=1024` 固定**（generate_minutes_local.py:350）— Stage 3 で修正済みの
   空応答バグと同型。→ **A で修正**
2. **`--chunk-minutes 10` ハードコード**（pm_from_recording.sh:460 / transcribe_pipeline.py:517、
   関数既定は 30）— 短コンテキスト対策の 10 分刻み。brief/risk 同型の全文脈化候補 → C
3. **`/argus-today`・`/argus-draft` が旧切り詰め経路**（`_MAX_CHARS_PER_CHANNEL=20000`、
   pm_argus.py:95）— LOG が「today への適用が有望」と明記済みの未消化 → C
4. **max_tokens 100〜1024 の 7 箇所**（検索キーワード抽出・HyDE・クラスタ命名・patrol 外部シグナル・
   exec_summary・TTS/動画要約）— reasoning 系で空応答リスク → **A で 4096 化**
5. **consensus N=3 既定**（generate_minutes_local / transcribe_pipeline / pm_argus.py:1995）—
   導入根拠は docs/minutes_consensus.md に明記された gemma4 の出力揺れ。LLM 呼び出し 1→18 倍。
   glm-5.2 で N=1 vs N=3 の盲検 A/B で決着 → C
6. **トリアージ 2 段構え**（ingest/slack.py:428 ほか）— 弱モデル前提の「抽出と判定の分離」。
   1 パス統合の A/B → C。「レスポンス欠落→DROP」の取りこぼしリスクも同時に検証
7. **決定事項「最大 5 件」上限**（generate_minutes_local.py:118 ほか）— V4-Flash の多弁対策の遺物 → C
8. **検索 top-5 × 400 字プレビュー**（retrieval.py:21-22,587 / mcp_tools.py:234）— LOG が名指しした
   gemma4 前提。re-rank 有効化済みの今、top-k/本文量拡大の A/B が次の一手 → C
9. **`--file` 全文 QA の 24k 字窓**（pm_argus_agent.py:56、400k 予算に対し 1/15 刻み）— 窓拡大で
   3 段の誤却下ガードごと簡素化できる可能性 → C
10. **Slack 抽出の think=True / base_t=0.4**（V4-Flash 期チューニング）— investigate は 07-18 に
    think=False へ決着済み。同じ再検証 → C
11. **docs の乖離**（argus_system.md の gemma4 優先/OPENAI_API_BASE/re-rank「無効」節・max_steps 5、
    minutes_consensus.md 全編、argus_outcomes.md:296,329、CLAUDE.md:39、commands.md:709）→ **A で修正**

## 判定保留（痕跡のみ列挙）

小型モデル向けの過剰な形式強制（出力テンプレート全文指定・同一例示の 3 箇所重複・後処理正規表現
矯正 8 本・CoT 足場）は glm-5.2 では逆効果の可能性があるが、変更は品質 A/B とセットでのみ行う。
詳細は本監査の元データ（会話ログ 2026-07-24）参照。
